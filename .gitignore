node_modules/
dist/
.env
.DS_Store
logs/**/*.md
logs/usage.jsonl
!logs/**/.gitkeep

# 実顧客データは PII のためコミットしない。列定義は data/customers.template.csv を参照
data/customers.csv

# 生成した動画(容量が大きいのでコミットしない)
output/

# エモーショナル・ムービーの素材(ジム実写真・採用画像。容量とプライバシーのためコミットしない)
assets/movie/

# 基準画像そのものはコミットしない（フォルダの形とREADMEだけ共有）
assets/reel_inputs/**
!assets/reel_inputs/README.md
!assets/reel_inputs/**/.gitkeep

# 量産の台帳（実行ごとに増えるのでコミットしない）
logs/factory.jsonl
